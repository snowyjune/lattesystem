
/*  
  function resizeContent() {
	var size_h = document.documentElement.clientHeight;
	var size_w = document.documentElement.clientWidth;

	var size_sh = document.documentElement.scrollHeight;//scrollHeight
	
	var size;
	if (size_h > size_sh) {
		size = size_h;
	} else {
		size = size_sh;
	}
	if (size > 600) {
        	document.getElementById("content").style.height = size + "px";
		document.getElementById("menu").style.height = size + "px";
		document.getElementById("sub-menu").style.height = size + "px";
	} else if (size_h <= 600) {
        	document.getElementById("content").style.height = "600px";
		document.getElementById("menu").style.height = "600px";
				document.getElementById("sub-menu").style.height = "600px";

	}


	//var input = document.getElementById("t_id");
	//input.size= Math.floor(0.02*size2 - 3);
	//var input = document.getElementById("t_pass");
	//input.size= Math.floor(0.02*size2 - 3);
  }

  function login(f) {
	alert("로그인 되셨습니다");
  }
  */